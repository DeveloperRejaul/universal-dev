import { styled } from '@gluestack-style/react';
import { View, Pressable, Text, Image } from 'react-native';

export const GSBox = styled(View, {}, {});
export const GSPressable = styled(Pressable, {}, {});
export const GSText = styled(Text, {}, {});
export const GSImage = styled(Image, {}, {});
